# Microatividade 1 - if e else

#Crie uma variável chamada temperatura e atribua a ela o valor 29.

#Crie uma verificação, utilizando a condição if, 
#para checar se o valor da variável temperatura é menor que 30.

#Caso positivo, imprima na tela o texto: "A temperatura hoje está amena";

#Caso contrário, e utilizando a condição else, imprima na tela o texto: "Hoje está fazendo calor";

#Ainda no script, altere o valor da variável temperatura para 31, 
#salve a alteração e execute novamente o script.

temperatura = 31

if temperatura < 30:
    print ("A temperatura hoje está amena")
else:
    print("Hoje está fazendo calor")